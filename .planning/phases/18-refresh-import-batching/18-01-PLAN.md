---
phase: 18
plan: 18-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ingest.js
  - test/report.test.js
  - test/ingest.test.js
  - scripts/benchmark-storage.js
autonomous: true
requirements:
  - PERF-05
  - PERF-06
  - PERF-07
  - PERF-08
---

<objective>
Batch report-time refresh imports and skip unchanged configured sources without changing CLI report semantics.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/copilot-metrics-18-refresh-import-batching/18-CONTEXT.md
@.planning/phases/copilot-metrics-18-refresh-import-batching/18-RESEARCH.md
@.planning/phases/17-file-backed-sqlite-storage/17-02-SUMMARY.md
@.planning/phases/17-file-backed-sqlite-storage/17-03-SUMMARY.md
@src/ingest.js
@src/jsonl.js
@src/sqlite-store.js
@test/report.test.js
@test/ingest.test.js
</context>

<must_haves>
- PERF-05: Normal reports still auto-import appended telemetry/session data and skip unchanged configured sources.
- PERF-06: `--refresh` re-reads changed source files while avoiding unnecessary writes for unchanged checkpoints/diagnostics.
- PERF-07: Report-time source-set refresh work uses shared database connection batching where practical.
- PERF-08: VS Code chat-session debug sidecar fingerprints remain part of refresh decisions.
- Do not change report output contracts, selected-price semantics, label confidence ranking, or manual-label precedence.
</must_haves>

<tasks>

<task id="18-01-01" type="auto">
  <name>Persist and use file-stat checkpoints for generic sources</name>
  <files>src/ingest.js, test/report.test.js, test/ingest.test.js</files>
  <action>
    Generalize file-stat checkpoint use so unchanged configured OTel, hook, and Copilot session sources can be skipped before JSON parsing. Keep VS Code chat debug-sidecar logic intact. For changed files under `--refresh`, parse from line 0 and merge idempotently. Persist checkpoint file-stat context after imports and after no-op changed-file reads.
  </action>
  <verify>
    <automated>node --test test/report.test.js test/ingest.test.js</automated>
  </verify>
</task>

<task id="18-01-02" type="auto">
  <name>Batch report auto-import storage work across source sets</name>
  <files>src/ingest.js</files>
  <action>
    Wrap report-time configured-source import processing and final repair passes in `runImportMutationBatch` so large source sets reuse a native storage connection. Keep file discovery before the batch and preserve the existing import result shape.
  </action>
  <verify>
    <automated>node --test test/report.test.js test/ingest.test.js</automated>
  </verify>
</task>

<task id="18-01-03" type="auto">
  <name>Verify refresh performance evidence</name>
  <files>scripts/benchmark-storage.js, test/report.test.js</files>
  <action>
    Extend tests or benchmark evidence to demonstrate unchanged-source skip behavior and the shared connection path. Keep normal report output free of performance instrumentation.
  </action>
  <verify>
    <automated>npm run benchmark:storage && npm test && npm run check</automated>
  </verify>
</task>

</tasks>

<verification>
- `node --test test/report.test.js test/ingest.test.js`
- `npm run benchmark:storage`
- `npm test`
- `npm run check`
- `npm run verify:package`
- `npm run verify:native-sqlite`
</verification>

<success_criteria>
- Normal report commands skip unchanged configured sources and still import appended data.
- Explicit `--refresh` re-reads changed files and preserves debug-sidecar cache evidence behavior.
- Copilot session-state/report auto-import work reuses shared database connection batching.
- Existing report JSON/human semantics remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/copilot-metrics-18-refresh-import-batching/18-01-SUMMARY.md` when done.
</output>
