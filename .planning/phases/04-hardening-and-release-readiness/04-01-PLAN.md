---
phase: 04-hardening-and-release-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - LICENSE
  - CHANGELOG.md
  - README.md
  - RELEASE.md
  - scripts/smoke.js
  - scripts/verify-package.js
  - scripts/manual-copilot-cli-flow.js
  - .github/workflows/npm-publish.yml
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - VERIFY-01
  - VERIFY-02
  - VERIFY-03
  - VERIFY-04
  - VERIFY-05
  - VERIFY-06
  - PUBLISH-01
  - PUBLISH-02
  - PUBLISH-03
  - PUBLISH-04
  - PUBLISH-05
user_setup:
  - Manual Copilot CLI validation may require an authenticated Copilot CLI on this machine. It must not run in CI.
must_haves:
  truths:
    - Package version remains `0.1.0` for the first release candidate.
    - Package metadata and allowlist make npm publishing safe: `.planning`, `.codex`, local telemetry, and fixture data are not packed.
    - MIT license file exists and package metadata points at MIT.
    - README describes the current tool state and does not read like a phase history.
    - Custom label extractors are configured through local config and do not require modifying package source.
    - CHANGELOG.md contains one aggregated `0.1.0` entry.
    - GitHub Actions run install, `npm test`, `npm run check`, smoke verification, and package verification without invoking real Copilot CLI.
    - A manual local Copilot CLI validation path sets up an example workspace, installs/configures `copilot-metrics`, installs hooks and Copilot config for the workspace, runs a simple prompt, and validates collected data.
    - Release evidence records automated checks, pack validation, smoke report output, and manual Copilot CLI validation status.
  artifacts:
    - package.json
    - LICENSE
    - CHANGELOG.md
    - README.md
    - RELEASE.md
    - scripts/smoke.js
    - scripts/verify-package.js
    - scripts/manual-copilot-cli-flow.js
    - .github/workflows/npm-publish.yml
  key_links:
    - GitHub Actions use npm scripts from package.json.
    - RELEASE.md documents GitHub Actions npm publish steps triggered by a human-created GitHub release/tag.
    - Manual Copilot CLI validation script uses existing CLI setup and import/report commands.
---

<objective>
Prepare `copilot-metrics@0.1.0` for the first npm release candidate with safe package contents, release docs, automated smoke checks, CI hardening, and manual local Copilot CLI validation.
</objective>

<execution_context>
@/home/nnex/dev/prj/copilot-metrics/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/copilot-metrics/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-hardening-and-release-readiness/04-CONTEXT.md
@.planning/phases/04-hardening-and-release-readiness/04-RESEARCH.md
@.planning/phases/03-jira-label-attribution-and-cli-querying/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden npm package metadata and pack contents</name>
  <read_first>
    - package.json
    - README.md
    - .github/workflows/npm-publish.yml
    - .planning/phases/04-hardening-and-release-readiness/04-CONTEXT.md
  </read_first>
  <files>package.json, LICENSE, CHANGELOG.md, RELEASE.md, scripts/verify-package.js, .github/workflows/npm-publish.yml</files>
  <action>Add MIT LICENSE, package metadata needed for npm publication, a strict `files` allowlist, package verification script, release checklist, and changelog. Update GitHub Actions to run install, tests, syntax check, smoke verification, and package verification before the automated npm publish job triggered by GitHub release/tag.</action>
  <acceptance_criteria>
    - `package.json` version is `0.1.0`.
    - `package.json` contains `license: MIT`, `bin`, `files`, repository metadata, and npm scripts for smoke and package verification.
    - `npm pack --dry-run --json` output does not include paths beginning with `.planning/`, `.codex/`, `test/`, or local telemetry/store data.
    - `LICENSE` contains MIT license text.
    - `CHANGELOG.md` contains a `0.1.0` entry.
    - `.github/workflows/npm-publish.yml` runs `npm ci`, `npm test`, `npm run check`, smoke verification, and package verification without Copilot CLI calls.
  </acceptance_criteria>
  <verify>npm run verify:package</verify>
</task>

<task type="auto">
  <name>Task 2: Add release smoke and manual Copilot CLI validation flow</name>
  <read_first>
    - src/cli.js
    - src/setup.js
    - src/hook-logger.js
    - test/fixtures/vscode-otel.jsonl
    - test/fixtures/copilot-cli-otel.jsonl
    - test/fixtures/hook-events.jsonl
  </read_first>
  <files>scripts/smoke.js, scripts/manual-copilot-cli-flow.js, package.json, RELEASE.md</files>
  <action>Add a CI-safe smoke script that initializes a temp home, imports fixtures, runs reports, and asserts expected labels/totals. Add a manual local Copilot CLI validation helper/documentation that creates an example workspace, configures this package's telemetry and hooks for the workspace, instructs/runs a simple Copilot prompt when available, then validates that telemetry/hook JSONL data exists and can be imported/reported.</action>
  <acceptance_criteria>
    - `npm run smoke` exits 0 without requiring Copilot CLI or network access.
    - Manual validation docs include example workspace setup, `copilot-metrics` install/config commands, hook install, Copilot CLI telemetry configuration, simple prompt execution, and data validation commands.
    - Manual helper does not run in CI by default.
    - Manual helper supports a dry-run/setup mode that can be checked without authenticated Copilot CLI.
  </acceptance_criteria>
  <verify>npm run smoke</verify>
</task>

<task type="auto">
  <name>Task 3: Refresh current-state docs and release evidence</name>
  <read_first>
    - README.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/04-hardening-and-release-readiness/04-CONTEXT.md
  </read_first>
  <files>README.md, RELEASE.md, .planning/REQUIREMENTS.md</files>
  <action>Rewrite README as current-state product docs for install, setup, privacy, import, reports, estimates, custom extractors, and release limitations. Ensure release checklist and requirements reflect that publishing readiness is included in Phase 4 for `0.1.0`.</action>
  <acceptance_criteria>
    - README documents install/use through npm/npx, setup, hooks, import, reports, privacy defaults, configurable custom extractors, estimates, and current limits.
    - README does not describe implementation phases as history.
    - RELEASE.md documents `npm pack`, npm auth/secrets, publish dry run, GitHub release/tag trigger, automated GitHub Actions npm publish, and post-publish verification.
    - Phase 4 requirement coverage includes verification and publishing readiness items needed for `0.1.0`.
  </acceptance_criteria>
  <verify>npm test && npm run check && npm run smoke && npm run verify:package</verify>
</task>

</tasks>

<verification>
- [ ] npm test
- [ ] npm run check
- [ ] npm run smoke
- [ ] npm run verify:package
- [ ] npm pack --dry-run --json
- [ ] Manual Copilot CLI validation attempted locally and status recorded
</verification>

<success_criteria>
- `copilot-metrics@0.1.0` package contents are safe to publish.
- CI validates the release candidate without real Copilot CLI calls.
- README, changelog, license, release checklist, and package metadata are publication-ready.
- Manual Copilot CLI data collection flow is documented and locally validated or explicitly recorded as blocked by missing external authentication/tooling.
</success_criteria>

<output>
After completion, create `.planning/phases/04-hardening-and-release-readiness/04-01-SUMMARY.md`
</output>
