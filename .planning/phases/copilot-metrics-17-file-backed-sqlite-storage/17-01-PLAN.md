---
phase: 17
plan: 17-01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/storage-backend.test.js
  - scripts/verify-native-sqlite-package.js
  - scripts/benchmark-storage.js
  - scripts/verify-package.js
  - package.json
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
must_haves:
  truths:
    - "PERF-01: Existing setup, import, label, and report commands have executable equivalence tests before backend migration."
    - "PERF-02: Schema, migrations, constraints, checkpoints, manual labels, selected pricing, diagnostics, and warning rows have backend equivalence assertions."
    - "PERF-03: Transaction and no-full-export behavior has an automated static/runtime gate."
    - "PERF-04: Native dependency install and packed-package CLI workflows have an automated smoke script."
  artifacts:
    - path: "test/storage-backend.test.js"
      provides: "Storage backend equivalence and transaction-contract tests"
    - path: "scripts/verify-native-sqlite-package.js"
      provides: "Neutral-directory native addon and packed CLI smoke"
    - path: "scripts/benchmark-storage.js"
      provides: "Representative storage write/checkpoint benchmark"
  key_links:
    - from: "test/storage-backend.test.js"
      to: "src/sqlite-store.js"
      via: "require('../src/sqlite-store')"
      pattern: "sqlite-store"
    - from: "scripts/verify-native-sqlite-package.js"
      to: "bin/copilot-metrics.js"
      via: "packed CLI execution"
      pattern: "copilot-metrics"
---

<objective>
Create the Phase 17 validation harness before product-code migration.

Purpose: Make the file-backed SQLite migration executable against explicit behavior, security, package, and performance contracts.
Output: Storage backend tests, native package smoke, storage benchmark script, and package artifact guardrails.
</objective>

<execution_context>
@/home/nnex/dev/prj/copilot-metrics/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/copilot-metrics/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/copilot-metrics-17-file-backed-sqlite-storage/17-RESEARCH.md
@.planning/phases/copilot-metrics-17-file-backed-sqlite-storage/17-VALIDATION.md
@.planning/phases/copilot-metrics-17-file-backed-sqlite-storage/17-PATTERNS.md
@AGENTS.md
@src/sqlite-store.js
@src/ingest.js
@src/reports.js
@src/cli.js
@scripts/verify-package.js
@test/ingest.test.js
@test/report.test.js
@test/setup.test.js
</context>

<must_haves>
- PERF-01: Test setup/import/label/report compatibility without changing command syntax or output meaning.
- PERF-02: Test persistence equivalence for schema initialization, indexes, dedupe constraints, import checkpoints, manual labels, selected pricing fields, diagnostics JSON, and import warnings.
- PERF-03: Test that the migrated backend does not retain `sql.js` full-file export mechanics and that transaction callbacks stay synchronous.
- PERF-04: Provide package/native smoke coverage for `better-sqlite3@12.10.0` in local and neutral-directory workflows.
- Phase boundary: Do not add refresh source-skipping, refresh batching, label ranking cache, or report query optimization work; those remain Phase 18 and Phase 19.
</must_haves>

<tasks>

<task id="17-01-01" type="auto" tdd="true">
  <name>Task 1: Create storage backend equivalence tests</name>
  <files>test/storage-backend.test.js</files>
  <read_first>
    @.planning/phases/copilot-metrics-17-file-backed-sqlite-storage/17-VALIDATION.md
    @.planning/phases/copilot-metrics-17-file-backed-sqlite-storage/17-PATTERNS.md
    @src/sqlite-store.js
    @test/ingest.test.js
    @test/report.test.js
    @test/setup.test.js
  </read_first>
  <behavior>
    - A temp `COPILOT_METRICS_HOME` store can initialize through `initStore`, then persist and reopen data through the public async store facade.
    - Storage preserves `raw_records`, `usage_records`, `hook_events`, `label_evidence`, `manual_label_assignments`, `import_warnings`, and `import_checkpoints`.
    - Unique indexes still dedupe raw fingerprints, usage identities, hook events, and label evidence according to the existing store contract.
    - Manual label helpers preserve canonical labels and active assignments across reopen.
    - Selected pricing, displayed credits, inferred diagnostics, pricing diagnostics JSON, and warning rows survive reopen.
    - Backend static assertions fail if `src/sqlite-store.js` still imports `sql.js`, calls `db.export()`, or wraps `async` callbacks in `db.transaction`.
  </behavior>
  <action>
    Create `test/storage-backend.test.js` using Node built-in `node:test` and `node:assert/strict`. Use the existing temp-home style from `test/ingest.test.js` and `test/report.test.js`. Import only public helpers from `src/sqlite-store.js`; do not reach into private implementation state. Use bound-parameter helper calls where available, and use fixture-shaped records from existing ingest/report tests so selected pricing, manual labels, warning rows, and label evidence assertions reflect current behavior. Add static checks that read `src/sqlite-store.js` and reject `require('sql.js')`, `initSqlJs`, `db.export()`, `persistDatabase(`, and `db.transaction(async`.
  </action>
  <verify>
    <automated>node --check test/storage-backend.test.js && node --test test/storage-backend.test.js</automated>
  </verify>
  <acceptance_criteria>
    `test/storage-backend.test.js` exists, imports the public store facade, covers PERF-01/PERF-02/PERF-03 behaviors above, and its only failures before Plan 17-02 are the intentional backend migration assertions. After Plan 17-02, `node --test test/storage-backend.test.js` must pass without updating expected report semantics.
  </acceptance_criteria>
  <done>See <acceptance_criteria>.</done>
</task>

<task id="17-01-02" type="auto">
  <name>Task 2: Add native package smoke and storage benchmark contracts</name>
  <files>scripts/verify-native-sqlite-package.js, scripts/benchmark-storage.js, scripts/verify-package.js, package.json</files>
  <read_first>
    @.planning/phases/copilot-metrics-17-file-backed-sqlite-storage/17-RESEARCH.md
    @.planning/phases/copilot-metrics-17-file-backed-sqlite-storage/17-VALIDATION.md
    @.planning/spikes/001-sqlite-performance/write-benchmark.js
    @.planning/spikes/001-sqlite-performance/write-benchmark-results.json
    @scripts/verify-package.js
    @package.json
    @test/fixtures
  </read_first>
  <action>
    Create `scripts/verify-native-sqlite-package.js` in the existing script style. It must run `npm pack --silent --json`, install the produced tarball in a newly created temp directory, require `better-sqlite3`, run the packed `copilot-metrics` binary for `--help`, `store init --home <tmp-home>`, a fixture import, a manual label command where the fixture has a known session, and a JSON report command. Add a package script such as `verify:native-sqlite` that runs it. Create `scripts/benchmark-storage.js` from the spike write benchmark pattern to run a representative checkpoint/write workload against a copied temp store and print JSON with operation count, elapsed milliseconds, and baseline spike references. Update `scripts/verify-package.js` so packed artifacts are forbidden from including `*.sqlite`, `*.sqlite-wal`, `*.sqlite-shm`, temporary DB files, or copied benchmark stores.
  </action>
  <verify>
    <automated>node --check scripts/verify-native-sqlite-package.js && node --check scripts/benchmark-storage.js && npm run verify:package</automated>
  </verify>
  <acceptance_criteria>
    `package.json` exposes runnable package/native and benchmark scripts, `scripts/verify-package.js` blocks SQLite database artifacts including WAL/SHM sidecars, and the new scripts are syntactically valid before Plan 17-02. After Plan 17-02 and Plan 17-03, `npm run verify:native-sqlite` and the benchmark script must run successfully from a neutral temp directory.
  </acceptance_criteria>
  <done>See <acceptance_criteria>.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/test input -> SQLite | Labels, session IDs, source paths, diagnostics, and fixture payloads enter SQL statements. |
| Local filesystem -> package artifact | Store files, WAL/SHM sidecars, and temp benchmark copies must not leak into npm package contents. |
| npm install -> local machine | `better-sqlite3` uses a native install script and must be validated as a pinned dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | `src/sqlite-store.js` queries | mitigate | Tests must exercise labels/session IDs/source paths through public helpers and static checks must require prepared statements/bound parameters for user-controlled values. |
| T-17-02 | Information Disclosure | `paths.usageDb`, WAL/SHM sidecars, package tarball | mitigate | Tests verify owner-local store creation where possible and package verification forbids `.sqlite`, `.sqlite-wal`, and `.sqlite-shm` artifacts. |
| T-17-03 | Elevation of Privilege | `better-sqlite3` native install script | mitigate | Pin `better-sqlite3@12.10.0`; native smoke installs packed tarball in a temp directory and requires the native addon before CLI execution. |
| T-17-SC | Tampering | npm package install | mitigate | Research package legitimacy audit is present and approved; any new `[ASSUMED]` or `[SUS]` package must block behind human verification. |
</threat_model>

<verification>
- `node --check test/storage-backend.test.js`
- `node --check scripts/verify-native-sqlite-package.js`
- `node --check scripts/benchmark-storage.js`
- `npm run verify:package`
</verification>

<success_criteria>
- Wave 0 creates explicit tests/scripts for every missing validation gap listed in `17-VALIDATION.md`.
- No product code is migrated in this plan.
- Phase 18 refresh batching and Phase 19 report query/ranking optimization are not implemented here.
</success_criteria>

<output>
Create `.planning/phases/copilot-metrics-17-file-backed-sqlite-storage/17-01-SUMMARY.md` when done.
</output>
