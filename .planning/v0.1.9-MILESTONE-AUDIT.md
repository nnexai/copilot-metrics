---
milestone: v0.1.9
audited: 2026-06-02T12:00:00.000Z
status: passed
scores:
  requirements: 12/12
  phases: 1/1
  integration: 1/1
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 8
    items:
      - "Post-publish isolated npx validation remains a release workflow step after the GitHub Actions/npm publish gate."
nyquist:
  compliant_phases: []
  partial_phases: []
  missing_phases: [8]
  overall: discovery_only_missing
---

# v0.1.9 Milestone Audit

## Result

Milestone audit passed.

Phase 8 delivered the v0.1.9 pricing evidence milestone: observed local charge evidence, session-local price metadata, cache-read status, upper-bound estimates, VS Code debug-log cached-token support, report `--refresh`, pricing-basis report fields, compact human report basis markers, and release documentation.

## Requirements Coverage

| Requirement | Status | Evidence |
|-------------|--------|----------|
| PRICE-01 | satisfied | Copilot session-state imports preserve `totalNanoAiu`, request cost/count, and total premium request counters. |
| PRICE-02 | satisfied | VS Code fallback parser imports session-local `inputCost`, `outputCost`, `cacheCost`, and token-price metadata. |
| PRICE-03 | satisfied | Store schema separates actual charge, pricing metadata, estimated values, upper-bound values, basis, confidence, and diagnostics. |
| PRICE-04 | satisfied | Usage rows expose `cache_read_status` as `known`, `explicit_zero`, or `unknown`. |
| PRICE-05 | satisfied | Complete token buckets with known prices produce high-confidence estimates. |
| PRICE-06 | satisfied | Cache-unknown prompt/output rows produce upper-bound estimates. |
| PRICE-07 | satisfied | Reports and warnings distinguish unknown pricing, static/session pricing, included/zero evidence, and actual/estimate deltas. |
| PRICE-08 | satisfied | JSON reports expose pricing basis fields across label, model, repo, detail, and unattributed reports. |
| PRICE-09 | satisfied | Human reports keep compact tables with basis markers and estimate notes. |
| PRICE-10 | satisfied | Usage identity and duplicate merge preserve strongest pricing evidence for repeated source imports and `--refresh`. |
| PRICE-11 | satisfied | VS Code cache metadata and debug-log cache evidence are diagnostics unless numeric cached-token counts are present. |
| PRICE-12 | satisfied | Fallback parsing and diagnostics avoid persisting full prompt/assistant content; positive auth-like log persistence was not introduced. |

## Integration

- Import, store, pricing, and report paths are wired through one pricing classifier.
- Report `--refresh` re-reads configured sources from the beginning and merges stronger debug-log evidence into existing usage rows without duplicating the exchange.
- VS Code debug-log lookup supports the local observed layout `GitHub.copilot-chat/debug-logs/<session-id>/main.jsonl`.
- Existing setup-once auto-import behavior remains intact.

## Verification

Passed:

```bash
npm test
npm run check
npm run smoke
npm run verify:package
npm run check:readme-version
```

## Release Gate

The package is prepared as `0.1.9` and package verification passed. The isolated published-package `npx` validation must run after the existing human-gated GitHub Actions/npm publish workflow publishes `copilot-metrics@0.1.9`.
