# Release 0.5.2 Evidence Dedupe Fix

## Goal

Ship the refresh label-evidence dedupe fix as `copilot-metrics@0.5.2`.

## Steps

- Bump package version and sync README examples.
- Update changelog with the refresh evidence dedupe fixes.
- Run local release verification from `RELEASE.md`.
- Commit release metadata and push `main`.
- Create GitHub release `v0.5.2`.
- Verify GitHub Actions publish and published package behavior from a neutral directory.
