---
milestone: v0.2.0
audited: 2026-06-02T13:23:00.000Z
status: passed
scores:
  requirements: 13/13
  phases: 1/1
  integration: 1/1
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 9
    items:
      - "Post-publish isolated npx validation remains a release workflow step after the GitHub Actions/npm publish gate."
nyquist:
  compliant_phases: []
  partial_phases: []
  missing_phases: [9]
  overall: discovery_only_missing
---

# v0.2.0 Milestone Audit

## Result

Milestone audit passed.

Phase 9 delivered the v0.2.0 VS Code displayed-credit milestone: displayed-credit parsing from chat-session details, pricing precedence ahead of token estimates, `0x` display handling, inferred cache-read diagnostics, refresh merge upgrades, report JSON fields, compact human `display*` markers, and release documentation.

## Requirements Coverage

| Requirement | Status | Evidence |
|-------------|--------|----------|
| DISP-01 | satisfied | VS Code chat-session parser imports numeric `credit` and `credits` details. |
| DISP-02 | satisfied | `0x` details import as included/zero displayed evidence. |
| DISP-03 | satisfied | Store/report fields preserve displayed credits, display text, basis, session/request/response/model evidence, and import context. |
| DISP-04 | satisfied | Actual charge evidence remains selected above displayed credits. |
| DISP-05 | satisfied | Displayed credits are selected before complete or upper-bound token estimates when no actual charge exists. |
| DISP-06 | satisfied | Token estimates still populate when no actual or displayed evidence exists. |
| DISP-07 | satisfied | Cache-unknown prompt/output rows still produce upper-bound estimates when no displayed evidence exists. |
| DISP-08 | satisfied | Material displayed-vs-estimate deltas emit diagnostics while preserving both values. |
| DISP-09 | satisfied | Bounded inferred cache-read tokens are reported separately from observed `cache_read_tokens`. |
| DISP-10 | satisfied | Label, model, repo, detail, and unattributed JSON reports expose displayed-credit and inferred-cache fields. |
| DISP-11 | satisfied | Human reports compactly show `display*` pricing basis and keep estimate notes. |
| DISP-12 | satisfied | `--refresh` re-reads VS Code session files and upgrades matching rows without double-counting usage. |
| DISP-13 | satisfied | Fixture tests cover numeric credits, `0x`, actual-over-displayed precedence, conflicts, inferred cache math, refresh merge, and privacy-preserving parsing. |

## Integration

- VS Code fallback parsing, pricing classification, SQLite persistence, duplicate merge, and reports all use the same displayed-credit evidence fields.
- Existing actual-charge, high-confidence estimate, upper-bound estimate, and debug-log cache evidence paths continue to pass.
- `multiplierNumeric: 0` and model metadata remain diagnostic-only unless an explicit displayed `0x` detail exists.
- Raw VS Code chat content remains redacted in persisted raw records.

## Verification

Passed:

```bash
npm test
npm run check
npm run smoke
npm run verify:package
npm run check:readme-version
```

## Release Gate

The package is prepared as `0.2.0` and package verification passed. The isolated published-package `npx` validation must run after the existing human-gated GitHub Actions/npm publish workflow publishes `copilot-metrics@0.2.0`.
