---
phase: 20-incremental-jsonl-and-lightweight-collection
plan: "03"
type: execute
wave: 2
depends_on:
  - 20-01
  - 20-02
files_modified:
  - src/ingest.js
  - test/ingest.test.js
  - test/setup.test.js
  - scripts/benchmark-incremental-jsonl.js
  - package.json
autonomous: true
requirements:
  - COL-02
  - VER-01
must_haves:
  truths:
    - "Each resolved VS Code debug log is parsed no more than once during one source-file import."
    - "Every matching usage record with unknown cache-read status receives the same cached-token evidence and diagnostic without overwriting explicit token evidence."
    - "The complete Phase 20 fixture matrix covers append, unchanged, truncation, replacement/rotation, malformed complete lines, partial lines, legacy checkpoints, hook output, and debug reuse."
    - "Repeatable npm commands run incremental JSONL and hook-startup benchmarks without weakening functional assertions."
  artifacts:
    - path: "src/ingest.js"
      provides: "Per-import resolved-debug-log cache shared across VS Code usage records"
    - path: "test/ingest.test.js"
      provides: "Debug reuse/evidence tests and complete incremental recovery fixture matrix"
    - path: "test/setup.test.js"
      provides: "Complete hook output and compatibility fixture assertions"
    - path: "scripts/benchmark-incremental-jsonl.js"
      provides: "Representative complete-read versus incremental-append benchmark with equivalence checks"
    - path: "package.json"
      provides: "npm benchmark entrypoints for Phase 20 collection and incremental ingestion"
  key_links:
    - from: "src/ingest.js"
      to: "vscodeDebugFileForSession"
      via: "one per-import Map keys parsed results by resolved debug-file path"
      pattern: "(Map|cache).*(debug|Debug)"
    - from: "applyVscodeDebugCachedTokens"
      to: "normalizeVscodeFallbackUsage"
      via: "shared cached-token result enriches all eligible records before cost enrichment"
      pattern: "applyVscodeDebugCachedTokens"
    - from: "package.json"
      to: "scripts/benchmark-incremental-jsonl.js"
      via: "benchmark:ingest npm script"
      pattern: "benchmark:(ingest|incremental)"
---

<objective>
Reuse VS Code debug-log parsing within each import and close Phase 20 with a complete fixture/benchmark verification surface.

Purpose: Eliminate repeated sidecar parsing without changing cached-token evidence, then make every Phase 20 normal and recovery contract easy to rerun.
Output: Per-import debug cache, consolidated regression matrix, incremental benchmark, and npm benchmark commands.
</objective>

<execution_context>
@/home/nnex/dev/prj/copilot-metrics/.codex/gsd-core/workflows/execute-plan.md
@/home/nnex/dev/prj/copilot-metrics/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-incremental-jsonl-and-lightweight-collection/20-CONTEXT.md
@.planning/phases/20-incremental-jsonl-and-lightweight-collection/20-01-SUMMARY.md
@.planning/phases/20-incremental-jsonl-and-lightweight-collection/20-02-SUMMARY.md
@src/ingest.js
@test/ingest.test.js
@test/setup.test.js
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cache resolved VS Code debug evidence once per import</name>
  <files>src/ingest.js, test/ingest.test.js</files>
  <behavior>
    - Multiple usage records referencing one session/debug file trigger one `readJsonl` parse for that file during the import.
    - All unknown-cache records receive the shared cached-token total, status, and vscode_debug_cached_tokens diagnostic.
    - Records with known or explicitly-zero cache evidence are not overwritten.
    - Missing or malformed debug logs preserve current fallback behavior and warning/privacy contracts.
  </behavior>
  <action>Refactor `readVscodeDebugCachedTokens` and `applyVscodeDebugCachedTokens` so `ingestVscodeChatSessionFile` creates one import-scoped cache keyed by the resolved debug-file path. Cache both positive and absent results so repeated records do not re-resolve/reparse the same file. Keep aggregation semantics and the `vscode_debug_cached_tokens` diagnostic identical, and never carry the cache across separate source-file imports where a sidecar may change. Add a multi-request fixture and instrument the reader/resolver through an injectable helper or narrow exported test seam to assert one parse, while separately asserting every eligible stored usage row receives evidence.</action>
  <verify>
    <automated>node --test --test-name-pattern='VS Code debug|debug log|cachedTokens' test/ingest.test.js</automated>
  </verify>
  <done>One import parses each resolved debug sidecar at most once and preserves cached-token evidence for all matching usage rows without overwriting stronger evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete the fixture-backed Phase 20 verification matrix</name>
  <files>test/ingest.test.js, test/setup.test.js</files>
  <behavior>
    - The test suite explicitly names and covers append, unchanged, truncation, replacement/rotation, malformed complete line, trailing partial line, and legacy checkpoint cases.
    - Hook-only and legacy hook outputs are semantically equivalent and remain redacted.
    - Debug-log reuse is proven both by one-parse instrumentation and by evidence on every matching usage row.
    - Explicit refresh, absolute warning lines, fingerprints, identities, attribution, and content-capture defaults are asserted alongside performance-sensitive behavior.
  </behavior>
  <action>Audit the tests added by Plans 20-01 and 20-02 against VER-01 and the Phase 20 context, then add any missing named fixture/temp-file cases in the existing test files. Prefer direct behavioral assertions on imported rows, warnings, checkpoint context, spawned hook output, and parse counts. Ensure reset cases compare against clean complete-import results and that the hook tests exercise generated configuration rather than only calling `appendHookEvent` directly. Keep all tests hermetic with temporary homes/files and no real Copilot invocation.</action>
  <verify>
    <automated>node --test test/ingest.test.js test/setup.test.js</automated>
  </verify>
  <done>VER-01 has explicit automated coverage for every required incremental JSONL, hook, and debug-log behavior with privacy and compatibility contracts intact.</done>
</task>

<task type="auto">
  <name>Task 3: Add incremental refresh benchmark and npm verification entrypoints</name>
  <files>scripts/benchmark-incremental-jsonl.js, package.json</files>
  <action>Create a deterministic benchmark that builds a representative temporary JSONL history, imports it once, appends a small newline-complete batch, measures incremental refresh, and compares its result with a clean complete import of the final file. Emit JSON containing history/appended byte and record counts, complete and incremental elapsed milliseconds, bytes processed/read when instrumentation exposes them, speedup, and semantic-equivalence status; fail on row/warning/evidence drift. Register this script and `scripts/benchmark-hook-startup.js` under clear npm benchmark commands in `package.json`. Do not introduce pass/fail timing thresholds tied to one machine; correctness assertions are blocking and timings are evidence.</action>
  <verify>
    <automated>npm run benchmark:ingest -- 2000 10 && npm run benchmark:hooks -- 5 && npm test && npm run check</automated>
  </verify>
  <done>Both Phase 20 benchmarks are repeatable through npm, emit machine-readable performance/equivalence evidence, and the complete automated suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VS Code debug sidecar → usage enrichment | Local debug logs may be malformed, stale, or contain unrelated/sensitive diagnostic content. |
| Benchmark-generated histories → import/store comparison | Synthetic input must not bypass production parsing or hide semantic drift behind timing-only output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-09 | Tampering | Per-import debug cache | medium | mitigate | Key by resolved debug path, scope the Map to one source import, and test absent/positive results plus explicit evidence precedence. |
| T-20-10 | Information Disclosure | Debug-log parser | medium | mitigate | Extract only numeric cache-token evidence, retain no debug content, and assert persisted diagnostics contain no source payload text. |
| T-20-11 | Denial of Service | Repeated debug parsing | medium | mitigate | Cache positive and negative resolutions within the import and assert one parse across multiple usage records. |
| T-20-12 | Repudiation | Performance benchmark | low | mitigate | Emit input sizes, Node/package versions, equivalence status, and separate timing fields in machine-readable output. |
</threat_model>

<verification>
- `node --test test/ingest.test.js test/setup.test.js`
- `npm run benchmark:ingest -- 2000 10`
- `npm run benchmark:hooks -- 5`
- `npm test`
- `npm run check`
</verification>

<success_criteria>
- One source import parses each resolved VS Code debug log no more than once while preserving evidence on every eligible record.
- The named fixture matrix covers every VER-01 case and the locked compatibility/privacy behaviors.
- Both performance paths have npm-run machine-readable benchmarks with blocking semantic-equivalence checks.
</success_criteria>

<output>
Create `.planning/phases/20-incremental-jsonl-and-lightweight-collection/20-03-SUMMARY.md` when done.
</output>
