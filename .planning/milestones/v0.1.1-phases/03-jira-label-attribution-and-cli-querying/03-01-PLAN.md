---
phase: 03-jira-label-attribution-and-cli-querying
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/label-extractors.js
  - src/labels.js
  - src/reports.js
  - src/sqlite-store.js
  - src/ingest.js
  - src/cli.js
  - test/ingest.test.js
  - test/report.test.js
  - test/fixtures/vscode-otel.jsonl
  - test/fixtures/copilot-cli-otel.jsonl
  - test/fixtures/hook-events.jsonl
  - README.md
autonomous: true
requirements:
  - NORM-04
  - NORM-06
  - REPORT-01
  - REPORT-02
  - REPORT-03
  - REPORT-04
  - REPORT-05
  - REPORT-06
  - REPORT-07
  - REPORT-08
user_setup:
  - None. npm/npx/node dependencies are sufficient.
must_haves:
  truths:
    - Built-in label extraction finds canonical uppercase Jira IDs such as DEMO-12345 from hook labels, branch names, cwd/path values, repo metadata fields, and prompt/task metadata when present.
    - Label attribution is evidence-preserving and statistical: source type, source field, source value context, session, usage record, hook event, and confidence are retained so later analysis can decide primary vs sidetrack labels.
    - A custom extractor API accepts source type plus source data and returns zero or more labels without requiring changes to report code.
    - Full prompt content is not stored by default; reports expose safe source/session context only.
    - Multi-label records retain all label evidence and report shared/multi-label context.
    - CLI reports exist for labels, a single label summary, single label detail, models, repos/directories, and unattributed usage.
    - Every report supports compact human output and `--json` machine-readable output.
    - Cost output remains explicitly labeled as an estimate.
  artifacts:
    - src/label-extractors.js
    - src/labels.js
    - src/reports.js
    - src/sqlite-store.js
    - src/ingest.js
    - src/cli.js
    - test/report.test.js
  key_links:
    - src/ingest.js calls label extraction while importing usage records and hook events.
    - src/sqlite-store.js owns the label_evidence table and report queries use that table through src/reports.js.
    - src/cli.js exposes `report ...` commands through src/reports.js.
---

<objective>
Implement Jira label attribution, evidence-preserving local reporting, and extensible label extraction for the local Copilot metrics CLI.
</objective>

<execution_context>
@/home/nnex/dev/prj/copilot-metrics/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/copilot-metrics/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-jira-label-attribution-and-cli-querying/03-CONTEXT.md
@.planning/phases/03-jira-label-attribution-and-cli-querying/03-RESEARCH.md
@.planning/phases/02-otel-ingestion-normalization-and-cost-model/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add extensible label extraction and evidence storage</name>
  <read_first>
    - src/otel.js
    - src/ingest.js
    - src/sqlite-store.js
    - src/hook-logger.js
    - .planning/phases/03-jira-label-attribution-and-cli-querying/03-CONTEXT.md
  </read_first>
  <files>src/label-extractors.js, src/labels.js, src/sqlite-store.js, src/ingest.js</files>
  <action>Create a built-in Jira extractor that canonicalizes labels to uppercase IDs matching patterns like DEMO-12345. Add an extractor registry function that calls extractors with `sourceType` and `sourceData` and accepts zero or more labels. Add label evidence persistence with columns for label, source_type, source_field, source_value, confidence, usage_record_id, hook_event_id, session_id, repo, branch, cwd, and timestamp. Wire ingestion so usage records and hook events produce evidence from hook labels, branch, cwd/path, repo metadata, and safe prompt/task metadata fields when present.</action>
  <acceptance_criteria>
    - `src/label-extractors.js` exports a built-in Jira extractor and a way to run custom extractors with source type plus source data.
    - `src/sqlite-store.js` creates a `label_evidence` table.
    - Importing `test/fixtures/vscode-otel.jsonl` stores evidence for `DEMO-12345` from branch metadata.
    - Importing `test/fixtures/hook-events.jsonl` stores evidence linked to hook events and session IDs.
    - No imported row stores full prompt content by default.
  </acceptance_criteria>
  <verify>npm test</verify>
</task>

<task type="auto">
  <name>Task 2: Add report query commands</name>
  <read_first>
    - src/cli.js
    - src/sqlite-store.js
    - src/pricing.js
    - .planning/phases/03-jira-label-attribution-and-cli-querying/03-CONTEXT.md
  </read_first>
  <files>src/reports.js, src/sqlite-store.js, src/cli.js</files>
  <action>Add `copilot-metrics report labels`, `copilot-metrics report label <id>`, `copilot-metrics report label <id> --detail`, `copilot-metrics report models`, `copilot-metrics report repos`, and `copilot-metrics report unattributed`. Each command must support default human output and existing `--json` output. Reports must aggregate local SQLite data only and include estimate labels for cost totals.</action>
  <acceptance_criteria>
    - `node bin/copilot-metrics.js report labels --home <tmp> --json` returns discovered labels with sessions, token totals, estimated credits, first seen, last seen, and evidence counts.
    - `node bin/copilot-metrics.js report label DEMO-12345 --home <tmp> --json` returns summary totals for that label.
    - `node bin/copilot-metrics.js report label DEMO-12345 --detail --home <tmp> --json` returns usage/evidence rows with source and session context.
    - `node bin/copilot-metrics.js report models --home <tmp> --json` groups usage by model.
    - `node bin/copilot-metrics.js report repos --home <tmp> --json` groups usage by repo and cwd.
    - `node bin/copilot-metrics.js report unattributed --home <tmp> --json` shows usage without label evidence and includes safe session/source context.
    - Human output contains compact tables and the phrase `Costs are estimates`.
  </acceptance_criteria>
  <verify>npm test</verify>
</task>

<task type="auto">
  <name>Task 3: Add fixtures, tests, docs, and smoke verification</name>
  <read_first>
    - test/ingest.test.js
    - test/fixtures/vscode-otel.jsonl
    - test/fixtures/copilot-cli-otel.jsonl
    - test/fixtures/hook-events.jsonl
    - README.md
  </read_first>
  <files>test/ingest.test.js, test/report.test.js, test/fixtures/*.jsonl, README.md</files>
  <action>Expand fixtures to include `DEMO-12345` and a second label for multi-label/statistical attribution. Add tests for extraction sources, evidence preservation, custom extractor behavior, report JSON shape, human output smoke, unattributed usage, and estimate labeling. Update README with report commands, JSON output examples, privacy notes, and custom extractor extension guidance.</action>
  <acceptance_criteria>
    - Tests cover Jira extraction from branch, cwd/path, hook labels, and safe metadata fields.
    - Tests cover a custom extractor returning zero or more labels.
    - Tests prove multi-label evidence is preserved instead of reduced to a single primary label.
    - Tests prove unattributed usage is visible without prompt content.
    - README documents `report labels`, `report label`, `report models`, `report repos`, `report unattributed`, `--json`, estimate caveats, and custom extractor guidance.
    - `npm test && npm run check` exits 0.
  </acceptance_criteria>
  <verify>npm test && npm run check</verify>
</task>

</tasks>

<verification>
- [ ] npm test
- [ ] npm run check
- [ ] node bin/copilot-metrics.js import --source vscode --file test/fixtures/vscode-otel.jsonl --home /tmp/copilot-metrics-phase3 --json
- [ ] node bin/copilot-metrics.js import --source hooks --file test/fixtures/hook-events.jsonl --home /tmp/copilot-metrics-phase3 --json
- [ ] node bin/copilot-metrics.js report labels --home /tmp/copilot-metrics-phase3 --json
- [ ] node bin/copilot-metrics.js report label DEMO-12345 --home /tmp/copilot-metrics-phase3 --json
- [ ] node bin/copilot-metrics.js report unattributed --home /tmp/copilot-metrics-phase3
</verification>

<success_criteria>
- Phase 3 requirements are covered by tests and local CLI smoke checks.
- Label attribution is evidence-preserving and suitable for downstream statistical analysis.
- Custom extractor support exists without changing report logic.
- Reports provide both compact human output and stable JSON output.
- Costs remain clearly labeled as estimates.
</success_criteria>

<output>
After completion, create `.planning/phases/03-jira-label-attribution-and-cli-querying/03-01-SUMMARY.md`
</output>
