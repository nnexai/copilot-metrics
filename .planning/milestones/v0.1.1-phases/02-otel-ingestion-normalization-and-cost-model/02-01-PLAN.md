---
phase: 02-otel-ingestion-normalization-and-cost-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/paths.js
  - src/sqlite-store.js
  - src/jsonl.js
  - src/otel.js
  - src/pricing.js
  - src/ingest.js
  - src/cli.js
  - test/ingest.test.js
  - test/fixtures/vscode-otel.jsonl
  - test/fixtures/copilot-cli-otel.jsonl
  - test/fixtures/hook-events.jsonl
  - README.md
autonomous: true
requirements:
  - INGEST-01
  - INGEST-02
  - INGEST-03
  - INGEST-04
  - INGEST-05
  - NORM-01
  - NORM-02
  - NORM-03
  - NORM-05
  - COST-01
  - COST-02
  - COST-03
  - COST-04
  - COST-05
user_setup:
  - None. npm/npx/node dependencies are sufficient.
must_haves:
  truths:
    - CLI can initialize a SQLite store in the central user-level data directory.
    - CLI can import VS Code Copilot OTel JSONL, Copilot CLI OTel JSONL, and hook JSONL records.
    - Malformed JSONL rows are skipped and reported as warnings.
    - LLM/model-call spans are normalized separately from root agent/tool spans.
    - Token extraction handles input, output, cache read, cache creation, and reasoning token fields when present.
    - Known model records produce estimated USD and AI Credits using a versioned local pricing table.
    - Unknown models or missing pricing are stored with visible warnings instead of fabricated cost estimates.
    - Costs are labeled as estimates.
  artifacts:
    - src/sqlite-store.js
    - src/jsonl.js
    - src/otel.js
    - src/pricing.js
    - src/ingest.js
    - test/ingest.test.js
  key_links:
    - src/cli.js exposes import/store commands using src/ingest.js.
    - src/ingest.js uses src/otel.js for normalization and src/pricing.js for estimates.
    - src/sqlite-store.js owns schema creation and insert persistence.
---

<objective>
Implement local telemetry ingestion, normalization, SQLite persistence, and estimated GitHub AI Credit costing.
</objective>

<execution_context>
@/home/nnex/dev/prj/copilot-metrics/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/copilot-metrics/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-otel-ingestion-normalization-and-cost-model/02-CONTEXT.md
@.planning/phases/01-project-foundation-and-local-setup/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SQLite store and JSONL parsing</name>
  <files>src/paths.js, src/sqlite-store.js, src/jsonl.js</files>
  <action>Add `usageDb` to resolved paths. Implement JSONL parsing that returns records plus skipped-row warnings. Implement SQLite schema creation and insert helpers using portable npm dependencies only.</action>
  <verify>npm test</verify>
</task>

<task type="auto">
  <name>Task 2: Add OTel normalization and cost estimates</name>
  <files>src/otel.js, src/pricing.js, src/ingest.js</files>
  <action>Normalize spans from common OTel JSONL shapes, classify billing-like LLM spans, extract token/model/context fields, avoid root/tool span double counting, and estimate cost/AI credits from GitHub Copilot model pricing.</action>
  <verify>npm test</verify>
</task>

<task type="auto">
  <name>Task 3: Wire CLI, fixtures, docs, and verification</name>
  <files>src/cli.js, test/ingest.test.js, test/fixtures/*.jsonl, README.md</files>
  <action>Add `store init`, `pricing list`, and `import` commands with human and JSON output. Add fixtures and tests for VS Code OTel, Copilot CLI OTel, hook JSONL, malformed rows, double-count prevention, and unknown pricing warnings. Update README.</action>
  <verify>npm test && npm run check</verify>
</task>

</tasks>

<verification>
- [ ] npm test
- [ ] npm run check
- [ ] node bin/copilot-metrics.js store init --home /tmp/copilot-metrics-phase2 --json
- [ ] node bin/copilot-metrics.js import --source vscode --file test/fixtures/vscode-otel.jsonl --home /tmp/copilot-metrics-phase2 --json
</verification>

<success_criteria>
- Phase 2 requirements are covered with fixture-based tests.
- SQLite store can be initialized and queried by later phases.
- Unknown pricing and malformed records produce warnings.
- All costs are estimate-labeled.
</success_criteria>

<output>
After completion, create `.planning/phases/02-otel-ingestion-normalization-and-cost-model/02-01-SUMMARY.md`
</output>
