---
phase: 01-project-foundation-and-local-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bin/copilot-metrics.js
  - src/cli.js
  - src/paths.js
  - src/setup.js
  - src/hook-logger.js
  - test/setup.test.js
  - README.md
  - skills/copilot-metrics/SKILL.md
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04
  - SETUP-05
user_setup: []
must_haves:
  truths:
    - package.json exposes npm scripts for test, check, and CLI execution.
    - The CLI can be invoked through the package bin and supports setup plus hook preview/install commands.
    - The default data directory is user-level and can be overridden for tests or custom installs.
    - VS Code and Copilot CLI setup output points telemetry JSONL files at the central data directory with content capture disabled by default.
    - Hook setup supports local repository and user-global modes.
    - Hook logging stores redacted JSONL attribution metadata without full prompt content by default.
    - A Copilot Metrics skill document exists with instructions for LLMs to query local metrics.
  artifacts:
    - package.json
    - bin/copilot-metrics.js
    - src/cli.js
    - src/paths.js
    - src/setup.js
    - src/hook-logger.js
    - test/setup.test.js
    - README.md
    - skills/copilot-metrics/SKILL.md
  key_links:
    - bin/copilot-metrics.js imports and runs src/cli.js.
    - src/cli.js uses src/setup.js for setup and hook commands.
    - src/setup.js uses src/paths.js for all default local paths.
    - Generated hook configuration invokes bin/copilot-metrics.js hook-log.
---

<objective>
Build the first runnable local CLI foundation for Copilot Metrics.

Purpose: Establish an npm/npx-friendly command surface that can initialize local storage, show telemetry setup guidance, install or preview Copilot CLI hooks, and provide an LLM skill instruction artifact.
Output: Node.js package scaffold, CLI implementation, setup helpers, hook logger, tests, README, and skill document.
</objective>

<execution_context>
@/home/nnex/dev/prj/copilot-metrics/.codex/get-shit-done/workflows/execute-plan.md
@/home/nnex/dev/prj/copilot-metrics/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation-and-local-setup/01-CONTEXT.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Node package and CLI skeleton</name>
  <files>package.json, bin/copilot-metrics.js, src/cli.js, src/paths.js</files>
  <read_first>.planning/phases/01-project-foundation-and-local-setup/01-CONTEXT.md, AGENTS.md</read_first>
  <action>Create a CommonJS Node.js package with a bin named `copilot-metrics`. Add npm scripts `test`, `check`, and `cli`. Implement path resolution with `COPILOT_METRICS_HOME` override and user-level defaults based on XDG/macOS/Windows conventions.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - package.json contains bin.copilot-metrics.
    - src/paths.js exports resolvePaths.
    - bin/copilot-metrics.js runs src/cli.js.
  </acceptance_criteria>
  <done>CLI can print help and path information.</done>
</task>

<task type="auto">
  <name>Task 2: Implement setup and hook commands</name>
  <files>src/cli.js, src/setup.js, src/hook-logger.js</files>
  <read_first>README.md if it exists</read_first>
  <action>Add commands: `init`, `paths`, `setup vscode`, `setup copilot-cli`, `hooks preview`, `hooks install --scope local|global`, and `hook-log`. Use current official setting/env names: VS Code `github.copilot.chat.otel.*`, Copilot CLI `COPILOT_OTEL_*`, and hook JSON format with lifecycle events. Hook install must write local `.github/hooks/copilot-metrics.json` or user-global `~/.copilot/hooks/copilot-metrics.json`; preview must print without writing. Hook logger must read JSON from stdin and append redacted JSONL metadata.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - setup output includes content capture disabled by default.
    - hook preview includes sessionStart, userPromptSubmitted, postToolUse, agentStop, and errorOccurred hook entries.
    - hook install creates target directories/files for local and global scopes.
    - hook-log appends JSONL without raw full prompt text by default.
  </acceptance_criteria>
  <done>Setup commands run locally and tests cover generated output shape.</done>
</task>

<task type="auto">
  <name>Task 3: Document usage and add LLM skill</name>
  <files>README.md, skills/copilot-metrics/SKILL.md, test/setup.test.js</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Write README setup examples for npm/npx usage, data directory defaults, VS Code/Copilot CLI telemetry setup, hook preview/install, privacy defaults, and current limits. Add a skill document that instructs LLMs to use the CLI to find paths, initialize storage, inspect reports, and avoid reading raw prompt content unless explicitly enabled. Add fixture-style tests for path resolution, setup snippets, hook config, and hook logger redaction.</action>
  <verify>npm test && npm run check</verify>
  <acceptance_criteria>
    - README includes `npx copilot-metrics` examples.
    - Skill document exists and avoids assuming official billing authority.
    - Tests pass through npm scripts.
  </acceptance_criteria>
  <done>Phase foundation is runnable and documented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm test
- [ ] npm run check
- [ ] node bin/copilot-metrics.js --help
- [ ] node bin/copilot-metrics.js hooks preview --scope local --json
</verification>

<success_criteria>

- All tasks completed.
- All verification checks pass.
- No full prompt content is stored by default.
- Setup helpers are runnable locally without remote services.
- The CLI surface can grow into Phase 2 ingestion and Phase 3 report/query commands.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation-and-local-setup/01-01-SUMMARY.md`
</output>
