{
  "name": "copilot-metrics",
  "version": "0.1.0",
  "description": "Local-first Copilot usage telemetry setup and reporting tools.",
  "type": "commonjs",
  "bin": {
    "copilot-metrics": "bin/copilot-metrics.js"
  },
  "scripts": {
    "cli": "node bin/copilot-metrics.js",
    "check": "node --check bin/copilot-metrics.js && node --check src/cli.js && node --check src/paths.js && node --check src/setup.js && node --check src/hook-logger.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
